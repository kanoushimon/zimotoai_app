<%= link_to gravatar_for(current_user, size: 50), current_user %>
<h1><%= current_user.name %></h1>
<br>
<span><%= link_to "プロフィールを確認する。", current_user %></span>
<br>
<span><%= pluralize(current_user.microposts.count, "個の思い出") %></span>